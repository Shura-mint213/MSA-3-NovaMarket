http {
    upstream logistics_backend {
        server logistics.company.com max_fails=5 fail_timeout=30s;
    }

    server {
        listen 8080;

        location /logistics/ {
            proxy_pass http://logistics_backend;
            
            # Таймауты согласно требованиям (3 секунды)
            proxy_connect_timeout 3s;
            proxy_read_timeout 3s;
            proxy_send_timeout 3s;

            # Обработка ошибок
            proxy_intercept_errors on;
            error_page 500 502 503 504 = @fallback;
        }

        location @fallback {
            add_header X-Circuit-Breaker "ture";
            default_type application/json
            return 200 "{\"status\": \"processing\", \"message\": \"Logistics service is temporarily unavailable. Please try again later.\"}";
        }
    }
}